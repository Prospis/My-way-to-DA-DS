# 📊 ВИЗУАЛИЗАЦИЯ: Какой график когда использовать

## 🎯 Главное правило: График должен отвечать на вопрос

❌ **Плохо:** "Построю bar chart, потому что умею"  
✅ **Хорошо:** "Хочу сравнить категории → bar chart"

---

## 1️⃣ BAR CHART (Столбчатая диаграмма)

### 📌 Когда использовать:
- Сравнить категории (кто/что больше?)
- Показать рейтинг/ранжирование
- Небольшое количество категорий (3-15)

### ❓ Отвечает на вопросы:
- Какая категория продаёт больше?
- Кто из продавцов лучший?
- Какой месяц принёс больше выручки?

### 📊 Примеры из бизнеса:
- Выручка по категориям товаров
- Продажи по регионам
- Рейтинг сотрудников по количеству сделок

### 💻 Код:
```python
# Вариант 1: Pandas
df.groupby('Category')['Sales'].sum().plot(kind='bar', color='skyblue')

# Вариант 2: Matplotlib
categories = ['Beauty', 'Clothing', 'Electronics']
sales = [143515, 155580, 156985]
plt.bar(categories, sales, color='skyblue')
```

### 🎨 Советы по дизайну:
- Используй один цвет для всех столбцов (если нет особого акцента)
- Добавь сетку по Y (для читаемости)
- Поверни подписи, если они длинные (`rotation=45`)
- Отсортируй от большего к меньшему (`.sort_values(ascending=False)`)

---

## 2️⃣ PIE CHART (Круговая диаграмма)

### 📌 Когда использовать:
- Показать доли/проценты от целого
- Есть 3-6 категорий (не больше!)
- Доли примерно равны (иначе используй bar chart)

### ❓ Отвечает на вопросы:
- Какая доля выручки от каждой категории?
- Сколько процентов клиентов — мужчины?

### 📊 Примеры из бизнеса:
- Распределение бюджета по отделам
- Доля каждого продукта в общих продажах
- Демография клиентов (М/Ж)

### 💻 Код:
```python
df.groupby('Category')['Sales'].sum().plot(kind='pie', autopct='%1.1f%%')
plt.ylabel('')  # Убрать подпись оси Y
```

### 🎨 Советы по дизайну:
- `autopct='%1.1f%%'` — показать проценты
- `startangle=90` — начать с вертикали (красивее)
- НЕ используй 3D pie charts (они искажают восприятие)
- Если больше 6 категорий — используй bar chart

### ⚠️ Когда НЕ использовать:
- Больше 6 категорий
- Нужно точное сравнение (для этого bar chart)
- Доли очень маленькие (1-2%)

---

## 3️⃣ LINE CHART (Линейный график)

### 📌 Когда использовать:
- Показать изменение во времени (тренд)
- Данные с датами/временем
- Непрерывные данные

### ❓ Отвечает на вопросы:
- Как менялись продажи по месяцам?
- Растёт или падает метрика?
- В какой момент был пик/провал?

### 📊 Примеры из бизнеса:
- Динамика продаж по месяцам
- Рост числа клиентов
- Изменение цены акций

### 💻 Код:
```python
plt.plot(months, sales, marker='o', linewidth=2, color='green')
plt.xlabel('Месяц')
plt.ylabel('Продажи')
plt.grid(alpha=0.3)
```

### 🎨 Советы по дизайну:
- `marker='o'` — точки на линии (легче читать)
- `linewidth=2` — толстая линия (видно лучше)
- Добавь сетку (`.grid()`)
- Используй хронологический порядок (январь → декабрь)

### 🔍 Как читать:
- **Линия вверх** = рост
- **Линия вниз** = падение
- **Пик** = максимум (май: 105 транзакций)
- **Провал** = минимум (сентябрь: 65 транзакций)

---

## 4️⃣ HISTOGRAM (Гистограмма)

### 📌 Когда использовать:
- Показать распределение числовой переменной
- Понять, как часто встречаются значения
- Найти выбросы

### ❓ Отвечает на вопросы:
- Какой возраст самый частый?
- Есть ли выбросы в ценах?
- Нормально ли распределены данные?

### 📊 Примеры из бизнеса:
- Распределение возраста клиентов
- Частота покупок (1 раз в месяц, 2 раза...)
- Распределение зарплат

### 💻 Код:
```python
plt.hist(df['Age'], bins=20, color='lightgreen', edgecolor='black')
plt.xlabel('Возраст')
plt.ylabel('Количество клиентов')
```

### 🎨 Параметры:
- `bins=20` — количество столбцов (больше bins = детальнее)
- `edgecolor='black'` — обводка столбцов (чтобы видеть границы)

### 🔍 Как читать:
- **Высокий столбец** = много людей этого возраста
- **Низкий столбец** = мало людей
- **Несколько пиков** = несколько групп (например, 20, 40, 60 лет)

---

## 5️⃣ HEATMAP (Тепловая карта)

### 📌 Когда использовать:
- Показать связи между переменными
- Корреляционная матрица
- Матрица значений (например, продажи по дням недели × месяцам)

### ❓ Отвечает на вопросы:
- Какие переменные связаны друг с другом?
- Влияет ли возраст на сумму покупки?
- Есть ли корреляция между ценой и количеством?

### 📊 Примеры из бизнеса:
- Корреляция между метриками (цена, количество, выручка)
- Продажи по часам × дням недели
- Зависимость конверсии от канала трафика

### 💻 Код:
```python
correlation_matrix = df[['Age', 'Price', 'Sales']].corr()
sns.heatmap(correlation_matrix, annot=True, cmap='coolwarm', fmt='.2f')
```

### 🔍 Как читать:
- **Красный цвет (+1.0)** = сильная положительная связь  
  → Если одна переменная растёт, другая тоже растёт
- **Белый цвет (0.0)** = нет связи  
  → Переменные независимы
- **Синий цвет (-1.0)** = отрицательная связь  
  → Если одна растёт, другая падает

### 📊 Пример интерпретации:
```
Total Amount ↔ Price per Unit: +0.85  
→ Чем выше цена товара, тем выше сумма покупки (логично!)

Age ↔ Total Amount: -0.06  
→ Возраст НЕ влияет на сумму покупки
```

---

## 6️⃣ BOX PLOT (Ящик с усами)

### 📌 Когда использовать:
- Сравнить распределение между группами
- Найти выбросы
- Показать медиану и разброс

### ❓ Отвечает на вопросы:
- Какая категория имеет больший разброс цен?
- Есть ли экстремально дорогие товары?
- Где типичные значения (медиана)?

### 📊 Примеры из бизнеса:
- Сравнить зарплаты в разных отделах
- Разброс цен по категориям товаров
- Время доставки по регионам

### 💻 Код:
```python
sns.boxplot(data=df, x='Category', y='Price', palette='Set2')
```

### 🔍 Как читать Box Plot:

```
         │ ← Максимум (без выбросов)
         │
      ┌──┴──┐
      │  —  │ ← Медиана (50-й перцентиль)
      │     │   Ящик = 50% данных (25-75 перцентили)
      └──┬──┘
         │
         │ ← Минимум (без выбросов)
         
       • ← Выброс (экстремально большое/маленькое значение)
```

**Элементы:**
- **Линия посередине ящика** = медиана (типичное значение)
- **Ящик** = 50% данных (от 25-го до 75-го перцентиля)
- **Усы** (линии вверх/вниз) = диапазон данных (без выбросов)
- **Точки за усами** = выбросы

### 📊 Пример интерпретации:
```
Beauty:
- Медиана: $450 (типичная покупка)
- Ящик: $400-500 (50% покупок в этом диапазоне)
- Усы: $350-550 (почти все покупки)
- Нет выбросов = нет экстремальных цен
```

---

## 🎯 ШПАРГАЛКА: Какой график выбрать?

| Вопрос | График |
|--------|--------|
| Какая категория продаёт больше? | Bar Chart |
| Какая доля каждой категории? | Pie Chart |
| Как менялись продажи по месяцам? | Line Chart |
| Какой возраст самый частый? | Histogram |
| Влияет ли возраст на цену? | Heatmap |
| Есть ли выбросы в ценах? | Box Plot |
| Сравнить разброс между группами? | Box Plot |
| Рейтинг продавцов | Bar Chart (отсортированный) |

---

## 📐 Универсальный шаблон графика

```python
# 1. РАЗМЕР
plt.figure(figsize=(10, 6))

# 2. ГРАФИК
[твой график здесь]

# 3. ЗАГОЛОВОК И ПОДПИСИ
plt.title('Заголовок графика', fontsize=16, fontweight='bold')
plt.xlabel('Ось X', fontsize=12)
plt.ylabel('Ось Y', fontsize=12)

# 4. ДОПОЛНИТЕЛЬНО
plt.xticks(rotation=45)  # Поворот меток (если нужно)
plt.grid(alpha=0.3)      # Сетка (для bar/line)
plt.tight_layout()       # Чтобы ничего не обрезалось

# 5. СОХРАНЕНИЕ И ПОКАЗ
plt.savefig('images/график.png', dpi=300, bbox_inches='tight')  # СНАЧАЛА
plt.show()  # ПОТОМ
```

---

## ⚠️ Частые ошибки

### ❌ Ошибка 1: Пустой график при сохранении
```python
plt.show()
plt.savefig('file.png')  # Сохранит ПУСТОЙ график!
```
✅ **Правильно:**
```python
plt.savefig('file.png')  # СНАЧАЛА сохрани
plt.show()               # ПОТОМ покажи
```

### ❌ Ошибка 2: Слишком много категорий в pie chart
```python
# 15 категорий в круговой диаграмме = нечитаемо
```
✅ **Правильно:** Используй bar chart для 7+ категорий

### ❌ Ошибка 3: Bar chart для трендов
```python
# Использовать bar chart для месяцев = плохо видно тренд
```
✅ **Правильно:** Line chart для временных данных

---

## 🎨 Цветовые палитры

```python
# Одиночный цвет
color='skyblue'
color='lightgreen'
color='coral'

# Палитры seaborn
palette='Set2'      # Пастельные цвета
palette='viridis'   # Градиент (жёлтый → синий)
palette='coolwarm'  # Для heatmap (синий-красный)
```

---

## 📖 Дополнительные ресурсы

- Matplotlib gallery: https://matplotlib.org/stable/gallery/index.html
- Seaborn gallery: https://seaborn.pydata.org/examples/index.html
- От данных к визуализации: https://www.data-to-viz.com/

---

**Сохрани этот файл и открывай перед созданием графиков!**